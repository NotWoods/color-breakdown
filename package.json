{
  "name": "vibrant-gui",
  "version": "0.0.0",
  "description": "Extract prominent colors from an image with this PWA tool",
  "main": "index.js",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint:js": "prettier --write \"**/*.js\"",
    "lint:css": "prettier --write \"css/**/*.css\" --parser css",
    "lint": "npm run lint:js && npm run lint:css",
    "serve": "http-server"
  },
  "author": "Tiger Oakes",
  "license": "MIT",
  "dependencies": {
    "idb": "^2.1.1",
    "node-vibrant": "^3.0.0"
  },
  "devDependencies": {
    "http-server": "^0.11.1",
    "prettier": "^1.11.1"
  },
  "prettier": {
    "singleQuote": true,
    "trailingComma": "es5"
  }
}
